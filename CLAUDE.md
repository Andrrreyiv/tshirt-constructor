# tshirt-constructor — конструктор футболок


## 🧭 ПЕРЕДАЧА 20.09, ночь — палитра цвета надписи (читать первым)

🟢 **ВЫЛОЖЕНО НА БОЕВОЙ и проверено, sha256 сошлись 10 из 10.** Клиент 20.09, глядя на
конструктор ФОРМЫ: «может тогда уже всё сделаем как здесь, сможете добавить эти же цвета
и убрать надпись Цвет надписи».

Сделано: полный RGB-пикер заменён теми же **семью свотчами**, что в форме (цвета сняты
с боевого `/constructor/colors.json`, лежат в конфиге как `textColors`); подпись
«Цвет надписи» убрана. Свотчи того же вида, что палитра цвета изделия выше.

☠️ **Это отменяет пункт U8 ТЗ** («цвет надписи — полный RGB-пикер», решение 22.07) по прямой
просьбе клиента. Покупатель больше НЕ может задать произвольный оттенок. Вернуть = вернуть
`input[type=color]` в `textField()`; в конфиге `textColor.mode` переключён `rgb` → `palette`,
прежнее значение записано в `_note`.
⚠️ Цвета лежат в КОНФИГЕ конструктора, а не в админке: в форме их правит плагин «Jetron цвета»,
здесь такой вкладки нет. Попросит менять сам — это отдельная работа в `jetron-tshirt-admin.php`.

**Токен `20260920b` → `20260920c`**, десять файлов (вместе с конфигом), рамка 3686 подтянута.
Тесты **180 → 181**: сторожа пикера переписаны под свотчи, добавлен сторож на отсутствие
подписи и пикера. **Замер живого, кэш выключен, 390 с тачем:** свотчей 7 с нужными hex,
подписи над рядом нет, пикера нет, выбор красного перекрасил уже набранную надпись
(`#111111` → `#e2001a`), кружок на кнопке «Цвет» догнал, версия `?v=20260920c`, ошибок 0.
⚠️ Снимок боевого снять не удалось — `Page.captureScreenshot` большой области упирается
в таймаут CDP; вид подтверждён замером и снимком стенда с тем же кодом.

💰 **Цена за все доработки 20.09 не называлась.** 🔴 Клиенту не написано, что готово.

---

## 🧭 ПЕРЕДАЧА 20.09, вечер — блок надписи по голосовому клиента (читать первым)

🟢 **ВЫЛОЖЕНО НА БОЕВОЙ и проверено, sha256 сошлись 9 из 9.** Клиент голосовым 14:42 сказал,
что утреннюю правку понял иначе: «сейчас у нас история повторяется, что кнопки опять у нас
в ширине самих блоков, я немножко не это имел в виду». Три его требования, все закрыты:
1. «Эту фразу добавить текст нужно вставить в поле, где написано например Маша… убрать надпись
   добавить текст, она не нужна» — подпись над полем убрана, слова стали подсказкой самого поля.
2. «Этот внутренний блок с полем продолжается и в себя вмещает эти две кнопки, они не выходят
   из этого блока… отдельно и на всю ширину самого этого блока» — сегмент лежит ВНУТРИ бежевой
   плашки `.design-row--text`, а не под ней.
3. «Вместо грудь и спина… кнопку шрифт в белом фоне залить, а цвет сделать залипшим» — это
   `.seg` + `.seg__btn--active`, ровно тот же переключатель, что «Грудь | Спина».

**Что в коде:** `textField()` собирает `.seg.text-seg` из двух `.seg__btn`; под подписью каждой
стоит значение (`<small>` с именем шрифта, `.text-seg__dot` с текущим цветом). Кружок
перекрашивается прямо во время тяги пикера — `render()` там звать нельзя, пикер закроется.
Какая панель раскрыта, по-прежнему помнит `this._textPanel`; аккордеон не тронут.
⚠️ Стили `.text-fc*` и `.design-row__caption` удалены целиком, вместе с их медиа-правилом.

**Токен `20260920a` → `20260920b`**, восемь файлов (он стоит в каждом ESM-импорте), рамка 3686
подтянута. Тесты **180/180**. Замер стенда 360 и 1440: сегмент в один ряд, имя шрифта
не обрезается, переполнения нет; залипание одно на двоих, повторное нажатие закрывает.
Снимок боевого — `docs/screens-2026-09-20/boevoj-futbolki-segment.png`.

💰 **Цена за доработки 20.09 не называлась** (в форме такой же вход стоил 6 000 ₽).
🔴 Клиенту не написано, что готово.

---

## 🧭 ПЕРЕДАЧА 20.09, утро — две кнопки надписи и видимое поле (замещено вечерней)


🟢 **Сделано и проверено на стенде, тесты 176 → 179/179.** Клиент 20.09 в переписке просил
о двух вещах сразу, обе закрыты одной правкой:
1. «Можете эти кнопки в конструктор футболок добавить» — те же две кнопки «Шрифт» и «Цвет»,
   что 20.09 сделаны в конструкторе ФОРМЫ вместо мелкой ссылки. Бейдж «Шрифт и цвет»
   (белая пилюля с зелёной галочкой) убран совсем, вместе со стилями `.text-badge*`.
2. «А где поле добавить текст, как-то его выделить, а то его по факту нет» — поле было
   `border: 0; background: none` на кремовой плашке и читалось как подпись. Теперь белое,
   с рамкой 1,5 px и подсветкой фокуса, как поля фамилии и номера в конструкторе формы.

**Что в коде:** `textField()` в `src/js/ui/TshirtApp.js` — ряд `.text-fc` из двух кнопок
`.text-fc__btn` под полем; на кнопке «Шрифт» стоит имя текущего шрифта (`currentFontName()`),
на «Цвет» — кружок `.text-fc__dot` с текущим цветом, он перекрашивается прямо во время тяги
пикера (там `render()` вызывать нельзя — пикер закроется под рукой).
⚠️ Какая панель раскрыта, помнит **`this._textPanel`** (`'font' | 'color' | ''`): `render()`
пересобирает поле целиком, и без этого список схлопывался бы после каждого выбора шрифта.
Аккордеон не тронут: панель по-прежнему одна на страницу, клик мимо её закрывает и сбрасывает
`_textPanel`. Выбор цвета остался полным RGB-пикером (U8), свотчей тут нет — в отличие от формы.

☠️ **Две грабли, обе уже стоили круга:**
- `display: flex` **перебивает атрибут `hidden`**: без правила
  `.text-opts[hidden], .font-list[hidden], .text-opts__color[hidden] { display: none; }`
  раскрыты обе панели сразу, а `el.hidden` при этом честно отвечает `true` — замер сходится,
  глаза нет. Правило стережёт тест в `tests/TextRestyle.test.js`.
- **Кегль для узких экранов обязан стоять НИЖЕ стилей кнопок.** Сначала положил
  `@media (max-width: 560px)` в общий мобильный блок вверху файла — при равной специфичности
  побеждает то, что позже, и правило молча не работало. Теперь медиа-блок идёт сразу после
  `.text-fc__dot`; на 360 px без него длинное «Prosto One» режется многоточием.

**Замер стенда, пять профилей** (1440 / 1045 / 768 / 375 с тачем / 360): кнопки всегда в один
ряд, 161×44 на десктопе и 153-161×46 на телефоне, ни одно из трёх имён шрифтов не обрезается,
поле ввода 304-637 px. Сквозной ход: набрал «МАША» → слой `text` создан → «Шрифт» раскрывает
список и он остаётся открытым после выбора → «Цвет» подменяет панель → повторное нажатие
закрывает. Смена шрифта доезжает до слоя (`fontId: oswald`), дефект 25.08 не вернулся.

🟢 **ВЫЛОЖЕНО НА БОЕВОЙ 20.09 и проверено.** Девять файлов, sha256 сошлись **9 из 9**
(`index.html`, `app.css`, `main.browser.js`, `TshirtApp.js`, `OrderSummary.js`,
`AdminOverrides.js`, `LibraryPanel.js`, `PrintEditor.js`, `zone-editor.browser.js`) плюс токен
рамки на странице 3686. Токен модулей **`20260913b` → `20260920a`**, вхождение на 3686 было
ровно одно, длина `content.raw` 1230 не изменилась, `id="jetronConstructor"` цел.
⚠️ Бампать пришлось ВОСЕМЬ файлов: токен стоит в каждом ESM-импорте, а не только в `index.html`.
**Замер живого с выключенным кэшем**, 1440 и 375 с тачем: в ряду виден текст «Добавить текст»,
подпись совпала с кнопкой «Добавить принт» по цвету, весу и кеглю (сверено computed-стилями),
поле 305×44 белое с рамкой и подсказкой «Например: МАША», две кнопки, старого бейджа нет,
«Шрифт» раскрывает 3 образца, «Цвет» подменяет панель, слой текста создаётся, ошибок 0.
Страница покупателя `/onlajn-konstruktor-futbolok/` отдаёт рамку с `?v=20260920a`, старого нет.

💰 **Цена за эту доработку не называлась.** В форме такая же работа стоила 6 000 ₽, здесь она
дешевле — перенос готового решения плюс правка поля. Назвать или зачесть — за Андреем.
🔴 **Клиенту не написано, что готово** — сообщить после решения по цене.

---

## 🧭 ПЕРЕДАЧА 13.09 — что осталось (читать первым)

⚫ **20.09 ВСЁ ВЛИТО в `main` (`2c902bc`)**: ветка `docs/handoff-2026-07-31` целиком внутри
(проверено `git merge-base --is-ancestor`, впереди 0), открытых PR нет, демо отдаёт `?v=20260920b`.
Прежнее предупреждение «ветка на 40 коммитов впереди main» снято.

🟢 **Разработки по текущему объёму не осталось.** Сделано, выложено и проверено на живом:
просмотр принта крупно (10.09) · админка «Плотности и фасоны» с ценами и скрытием кнопок ·
«Короткий рукав» → «Базовая» · третий фасон `long` · **приём заказа: покупатель может купить
футболку** (проверено end-to-end — 1550 ₽ × 2 = 3100 ₽ в корзине, макет приложен).

🔴 **Руки Андрея (по убыванию срочности):**
1. ✅ **Инструкция `docs/Инструкция-конструктор-футболок-2026-09-13.docx` ОТПРАВЛЕНА 13.09.**
   ⛔ Повторно не слать.
2. Выставить счёт. Согласовано: **5 000 ₽** (просмотр принта) + **10 000 ₽** (админка) +
   **12 000 ₽** (приём заказа) = **27 000 ₽** по футболкам. Форма идёт отдельно, 25 000 ₽.
3. Прежние долги: `docs/otchet-klientu-2026-08-31.md` не отправлен · КП по админке
   (18 000 / 30 000 ₽) не отправлено · снести `tshirt-backup-20260825-*.zip` из `public_html`
   руками (проверять листингом, а не HTTP-статусом) · вектор «РУКИ ВВЕРХ» ждёт двух слов
   (ширина печати и цена, рекомендовано 3 000 ₽).

🟡 **За клиентом:** прислать **6 фотографий** для «Длинного рукава» (3 цвета × 2 стороны) —
без них кнопка фасона у покупателя не появится; колонка цен «Длинный рукав» пока пустая.

📌 **Дальше по продукту, если клиент захочет:** категория товаров WooCommerce с подтягиванием
футболок. Не оценивалось. Смысл появляется только теперь, когда заказ работает.

Статус-хендофф. Обновлено: 2026-08-31. Self-contained контекст для любой сессии/аккаунта.

📚 **История лежит побайтово в двух файлах, ничего не выброшено:**
`docs/HISTORY-tshirt-2026-08-25.md` — работы 23.07-17.08 (88 421 байт, 775 строк, 28 разделов);
`docs/HISTORY-tshirt-2026-08-31.md` — **полная копия этого файла на 31.08 до сжатия**
(44 725 байт, 403 строки, обе волны правок по голосовым целиком, слово в слово).
Проверщик потерь 31.08: 358 содержательных строк, «ПОТЕРЬ НЕТ».
Нужна подробность по закрытой работе — она там, слово в слово. Здесь только живое.
⚠️ Папка `docs/` в .gitignore сознательно, поэтому файл истории лежит ЛОКАЛЬНО на этом
компе и в репозиторий не попадает — как и все прочие внутренние материалы проекта.

### Правило ведения этого файла
Закрытая работа сюда не пишется: строка о результате, подробности в HISTORY. Захотелось
третий абзац про закрытое — раздел едет в HISTORY целиком. Потолок 25 КБ: прежний файл
дорос до 88 КБ и съедал ~25k токенов в сессию до первого слова работы.

## Что это
Онлайн-конструктор футболок (vanilla JS, без сборки): тип (короткий рукав / оверсайз),
плотность, цвет, сторона (перёд/спина), принт из библиотеки или свой файл, надпись,
итог заказа. Сдан и принят клиентом 02.08.2026, дальше идут доработки по его замечаниям.

⚠️ **НЕ ПУТАТЬ с `jetron-constructor`** — это конструктор футбольной ФОРМЫ (линейки
Champion/Legend). Тот же заказчик (jetronsport.ru), но отдельный продукт и отдельное репо.

## Где что
- Демо: https://andrrreyiv.github.io/tshirt-constructor/ (GitHub Pages, деплой = push в main)
- Боевой: `https://jetronsport.ru/tshirt/`, страница покупателя `/onlajn-konstruktor-futbolok/` (ID **3686**)
- Конфиг и ценовое ядро: `src/config/tshirt-mock-config.json` (решения заказчика в `_note`-полях с датами)
- Код: `src/js/tshirt/*.js`, UI: `src/js/ui/TshirtApp.js`, стили: `src/css/app.css`
- Админка: `deploy/jetron-tshirt-admin.php` → `wp-content/mu-plugins/` на сервере
- Тесты: **137/137**. Прогон только через мост tdd-guard (см. ниже), не голым `node --test`

## ⚠️ ГЛАВНОЕ ПРАВИЛО ДЕПЛОЯ: версия в адресе iframe
Страница 3686 встраивает конструктор как `<iframe id="jetronConstructor" src=".../tshirt/?v=ТОКЕН">`.
**Залить файлы НЕДОСТАТОЧНО.** Не сменил ТОКЕН в адресе — вернувшийся посетитель получает
из кеша и старый `index.html`, и все модули с тем же `?v=`, то есть ВСЮ старую сборку.
Отсюда рождается «я обновляю, а ничего не меняется».
- ⚠️ **id рамки менять НЕЛЬЗЯ**: скрипт авто-высоты сайта ищет именно `jetronConstructor`.
- Токен правится через REST `POST /wp-json/wp/v2/pages/3686` (текстового поля в редакторе
  нет, страница собрана билдером, токен лежит в `content.raw`). Нонс: `GET /wp-admin/admin-ajax.php?action=rest-nonce`
  — обычный nonce со страницы отдаёт `rest_cookie_invalid_nonce`.
- Перед патчем убедиться, что вхождение токена РОВНО одно и длина контента не меняется.
- Токен бампать и для ЛОКАЛЬНОЙ проверки: ESM кешируется по URL, `location.reload()` не лечит.
- **Текущий токен: `20260920c`**, поднят 20.09 ночью вместе с палитрой цвета надписи (проверено на живом:
  рамка 3686 отдаёт `?v=20260920c`, старых вхождений 0). Прежние: `20260920b` (сегмент 20.09), `20260920a` (утренняя правка 20.09), `20260913b` (приём заказа),
  `20260913a` (админка 13.09), `20260910a` (просмотр принта 10.09), `20260826a`.
  ⚠️ До того на 3686 жил **`20260801d`**, а не `20260825a`, как врал этот файл: поймано
  только счётом вхождений. Правило: считать `content.raw`, а не верить записи о токене.
- ⚠️ **Заливка идёт ТОЛЬКО из живого Chrome владельца** через расширение Claude in Chrome.
  `chrome-devtools` MCP не подходит: свой чистый профиль, сессии WP нет, антибот отдаёт капчу.

### Механика заливки (WP File Manager, elFinder)
- 1R AntiBot режет curl, работать через Claude in Chrome. PUT и DELETE у REST антибот тоже
  режет (отдаёт HTML со статусом 200), POST проходит.
- Текст: `cmd=put`, **без** `encoding=scheme`, ОДИН файл за вызов (батч отклоняется классификатором).
- ⚠️ **`cmd=put` через `FormData` молча переводит переводы строк в CRLF** (файл тяжелеет ровно
  на число строк, SHA-256 расходится, сверка падает). Так же ведёт себя `URLSearchParams`:
  нормализация в CRLF заложена в спецификацию форм. Лечение: собирать тело urlencoded РУКАМИ
  через `encodeURIComponent` и слать с `Content-Type: application/x-www-form-urlencoded;
  charset=UTF-8` (замер 27.08: 10 из 10 файлов сошлись байт в байт).
- `cmd=put` пишет только в СУЩЕСТВУЮЩИЙ файл: под новый сначала `cmd=mkfile`.
- Бинарники (шрифты, картинки) только `cmd=upload` с FormData и `overwrite=1`, target = хеш КАТАЛОГА.
- Проверять НАЛИЧИЕ в листинге `cmd=open`, а не HTTP-статусом: антибот отдаёт свою HTML-страницу
  (~17 КБ) со статусом 200 вместо 404.
- Сверка деплоя надёжнее всего по SHA-256 через `crypto.subtle` в браузере.
  ⚠️ Правило «первый байт 0 = порча» верно только для текстовых файлов: TTF законно
  начинается с `0x00` (сигнатура sfnt).

## Настройки на сервере (их правит клиент, кодом не трогать)
`tshirt/admin.json` (цены печати и надписи), `tshirt/prints.json` (библиотека и тон),
`tshirt/zones.json` (зоны печати, ключ `child` — отдельная детская коробка),
`tshirt/crops.json` (кадр мокапа у каждой модели).
Наложение — `src/js/tshirt/AdminOverrides.js`: битый или пустой раздел игнорируется,
конструктор молча остаётся на базовом конфиге.
Редактор зон и кадра: `/tshirt/?zones=edit` (к КОРНЮ: `serve` роняет query на `/index.html`).
Гость панель увидит, но сохранить не сможет: экшены требуют `manage_options` и nonce.

## Ценообразование
- ⚠️ **ПОПРАВКА 13.09, замерено кодом: цена изделия НЕ тянется из карточки товара.**
  Обращений к WooCommerce в репозитории нет ни одного (`src/`, `deploy/` — только комментарии).
  База считается из `prices.form` конфига: `OrderBuilder.js:18` — `config.prices.form[type][density]`,
  фолбэк на первую плотность, иначе 0. «Из карточки товара» — договорённость на бумаге
  (`_note_form` в конфиге и шапка `jetron-tshirt-admin.php:12`), в коде не реализована.
  Матрица base 160=550/180=600/220=700/300=850, oversize 750/800/900/1050 — это рабочий
  источник цены, а не заглушка. ⚠️ И `prices.form` админкой НЕ перекрывается
  (`AdminOverrides.js:12-19` трогает только цены печати, надписи, цвета и изделия).
- Печать ступенчатая (`rule=min-containing`), два метода: DTF (40×40=1050, 40×50=1250)
  и плёнка (30×40=700, 40×50=800). Возраст задаёт рамку: adult 40×50, child 30×40.
- Текст: standalone **500 ₽** (подтверждено 28.07), при принте скидка 50%, явно не показывать.
- ⚠️ **Пути в корзину у конструктора футболок НЕТ** (`OrderBuilder.js`: «за блокером»).
  Когда кассу будут делать — НЕ подключать сумму из браузера к `set_price`. Готовый
  правильный образец: `jetron-constructor/deploy/jetron-orders.php`, копировать оттуда.

## 📌 ДВЕ ВОЛНЫ ПРАВОК ПО ГОЛОСОВЫМ КЛИЕНТА — ЗАКРЫТЫ, ВЫЛОЖЕНЫ, СВЕРЕНЫ

Разбор каждого пункта, все замеры и контроль в обе стороны лежат побайтово в
`docs/HISTORY-tshirt-2026-08-31.md`, разделы «ПЕРЕДАЧА 25.08» и «ПЕРЕДАЧА 26.08».
Здесь итог и то, что ещё будет стоить времени.

**25.08, четыре замечания** (видео и голосовое, расшифровка `docs/voice-client-2026-08-25.txt`).
Тесты 124 → 126. Коммит `e826285`, на боевом под токеном `20260825b`, 11 файлов сверены
по SHA-256 → 11 из 11.
1. Принт сам сжимался после изменения размера — замером НЕ воспроизвелось, геометрия
   стабильна, правки не потребовалось.
2. Смена шрифта не меняла уже набранную надпись — закрыто.
3. Метод нанесения навязывается, а не выбирается: надпись без принта — только «Плёнкой»,
   любой принт — только DTF, вторая кнопка убрана совсем (`src/js/tshirt/PrintMethod.js`).
   Причина у клиента денежная: «плёнкой дешевле, они будут тыкать плёнкой, а потом лишнее
   объяснять, что плёнкой это не получится».
4. «Ползёт вниз до бесконечности» — ДВЕ причины: окно библиотеки стояло `absolute`
   и `body { min-height: 100vh }` внутри рамки авто-высоты. Обе убраны.

**26.08, пять замечаний** (три скриншота и пять голосовых). Тесты 126 → **137/137**,
десять новых тестов в четырёх файлах. Коммиты `a15f0c1` и отдельно бамп `e24d4cf`,
на боевом под токеном `20260826a`, 10 файлов сверены по SHA-256 → 10 из 10.
1. Миниатюра съедала первую и последнюю букву («написано смирнов, а на миниатюре мирно»):
   кегль был привязан к ШИРИНЕ ОКНА через `clamp(6px, 2.2vw, 13px)`, переведён на
   контейнерные единицы (`container-type: size` + `font-size: 60cqh`), `clamp` оставлен
   выше запасным.
2. Названия шрифтов красились в цвет надписи — цвет убран из JS в двух местах и задан
   в CSS: `.font-opt__sample { color: var(--ink) }`.
3. Галочка в зелёном кружке стояла не по центру — повёрнутый на 45° `::after` заменён
   инлайновым SVG в `background`.
4. Удаление надписи на футболке не чистило поле ввода — `PrintEditor._deleteLayer`
   докладывает наверх через `onRemove(d)` → `TshirtApp.forgetLayer(d)`.
5. Раскрытые поля не схлопывались — новый `src/js/ui/PanelAccordion.js`, сворачивает
   ЛЮБОЙ клик снаружи, а не просто «по одному за раз».

### ⚠️ Что из этого нельзя забыть при следующей правке
- **Тихий режим набора надписи.** Панель нарочно НЕ пересобирается при вводе, иначе слетает
  фокус. Всё, что догоняет набор, вешать на `updatePrice()` и постоянное гнездо
  (`.method-slot`, `refreshMethodField()`), иначе оно появится только после клика по другому
  полю. Дыру нашли ГЛАЗАМИ, тесты её не видели. Побочно гнездо ломает `.field:last-child`,
  лечится `.method-slot:empty { margin-top: -16px }` (`:has()` моложе части браузеров).
- **`body` внутри рамки авто-высоты нельзя мерить в `vh`**: `100vh` там это высота САМОЙ
  рамки, сайт ставит рамке высоту документа, и петля растёт по 16px за кадр без конца.
  Сторож `tests/OverlayPin.test.js` не пускает `vh` обратно.
- **`documentElement.scrollHeight` внутри рамки не бывает меньше высоты рамки**, поэтому
  скрипт сайта умеет её только увеличивать. И он же, а не `body.scrollHeight`, видит
  `absolute`-оверлей: `body.scrollHeight` не растёт даже при `top: 3000px`.
- **Слушатели жеста висят на РУЧКЕ `.pf-print__handle`**, а не на window (`setPointerCapture`).
  Синтетический драг слать туда же, иначе «ничего не происходит» и это ложная тревога.
- **Ширина элемента слоя между шрифтами НЕ меняется** и это не откат на дефолт: элемент
  это коробка слоя, а не строка глифов. Проверять `document.fonts` и canvas `measureText`.
  ⚠️ Шрифтов в конфиге **три** (Russo One, Oswald, Prosto One), а не пять.
- **Дефект миниатюры не виден на полноразмерном слое**, ловится только на уменьшенном
  (`0.30×0.06`). Замер на крупном слое едва не дал ложное «всё и так работает».
- **`cqh`, а не `:has()`**: контейнерные запросы в Firefox с 110 (фев 2023), `:has()`
  только с 121 (дек 2023).
- **Аккордеон, четыре условия сразу:** слушатель только в фазе ПЕРЕХВАТА (иначе клик по
  чужой кнопке сперва откроет, а сторож тут же закроет); корнем регистрируется ВЕСЬ
  обёрточный `.field`, а не тело (иначе схлопывается выбор шрифта и перетаскивание пикера);
  `details.open` присваивать ТОЛЬКО при реальном расхождении, иначе рекурсия; событие
  `toggle` у `<details>` АСИНХРОННОЕ, мерить через `await` с паузой ≥60 мс.
- **Порядок в конструкторе редактора: `onRemove` идёт ПЕРЕД `onChange`**, потому что
  `onChange` пересобирает панель и прочитал бы старое значение. Поле чистим только когда
  `liveTextId` указывает на ЭТОТ слой: крестик на надписи с другой стороны поле не трогает.
- ⚠️ `docs/` целиком в `.gitignore` («внутренние материалы клиента»), поэтому под гитом НЕ
  лежат ни `docs/vector-rv/`, ни расшифровки голосовых, ни HISTORY. Копия у них ровно
  одна — на этом диске.

### 🔴 Осталось по обеим волнам (разработкой НЕ является)
1. **Резервный архив `tshirt-backup-20260825-4e89758cd207079e45b06d5b.zip` в `public_html`
   скачивается кем угодно по прямой ссылке** (замер 31.08: статус 206, сигнатура `PK`,
   5 976 420 б, вся папка `tshirt` до правок). Угадываемое имя убрано, старый адрес
   `/tshirt-backup-20260825.zip` отдаёт 403. Откат он не страхует: деплой сверен по SHA-256
   дважды. **Сносить владельцу руками** в Диспетчере файлов, мне безвозвратное удаление
   запрещено. ⚠️ Проверять удаление листингом `cmd=open`, а не HTTP-статусом.
2. **Отчёт клиенту по девяти пунктам** написан 31.08: `docs/otchet-klientu-2026-08-31.md`.
   Не отправлен, отправляет владелец.

## 🟢 10.09 — просмотр принта крупно: СДЕЛАНО И ВЫЛОЖЕНО

Голосовое клиента 09.09 в 14:19: принт при выборе не разглядеть, нужна кнопка увеличения
«как в Ютубе», но **не на весь экран**. Замер боевого подтвердил: плитка **138×138 px**
при файле 622×800…1596×1500, а клик по плитке только выбирал принт и закрывал окно.

Сделано: новый `src/js/tshirt/PrintPreview.js` + лупа и слой просмотра в `LibraryPanel.js`
+ стили `.libm__zoom` / `.libp*`. Стрелки листания и счётчик — сверх просьбы, согласовано.
**Тесты 137 → 151, провалов 0.** Замер стенда: 138×138 → **467×600**, Esc гасит сначала
просмотр и лишь потом библиотеку, телефон 375 чистый. 💰 **5 000 ₽ отдельной строкой**,
в счёт по конструктору ФОРМЫ не включать.

⚠️ **«Увеличить» ≠ «отдать оригинал»:** показываем ТОТ ЖЕ файл, что в плитке. В библиотеку
сознательно идёт уменьшенное превью вместо оригинала 3111×4000 — перепечатать с него нельзя.
⚠️ Лупа — СЕСТРА кнопки выбора (`.libm__cellwrap`), а не вложена в неё: `.libm__cell` сама
`<button>`, и клик всплывал бы в выбор принта.

🟢 **10.09 ВЫЛОЖЕНО НА БОЕВОЙ, сверка 11 из 11, провалов 0.** Залито 11 файлов: три
с правкой (`PrintPreview.js` — новый, через `mkfile`; `LibraryPanel.js`; `app.css`) и восемь
с общим бампом токена. Каждый сверен SHA-256 чтением ОБРАТНО с боевого. Бэкап боевых версий
до перезаписи — `C:/tmp/tshirt-backup-20260910/` (включая `page-3686-before.html`).
Токен рамки на 3686 поднят `20260826a` → `20260910a`.

⚠️ **Общий бамп был обязателен, а не «на всякий случай»:** замер 10.09 показал, что JS и CSS
на боевом отдаются с `Cache-Control: max-age=604800`. Без смены адреса вернувшийся покупатель
неделю брал бы старые модули из кеша, и правка до него бы не дошла.

**Замер живого боевого 10.09** (страница покупателя `/onlajn-konstruktor-futbolok/`): рамка
отдаёт `?v=20260910a`, плиток 82 — луп 82, плитка 138×138 → просмотр **622×422** при файле
800×800, счётчик «3 из 82» → «4 из 82» после листания, библиотека при открытом просмотре жива,
первый Esc гасит просмотр, второй — библиотеку. Ошибок в консоли по нашим файлам 0 (единственная
`ERR_BLOCKED_BY_CLIENT` — блокировщик рекламы на стороне браузера).

⚠️ Восемь файлов бампа ушли на боевой с LF: `sed -i` снял CRLF. Функционально всё равно,
сверка сошлась, боевой равен репозиторию — но знать стоит, прежние выкладки были CRLF.

✅ **Приёмочный прогон 10.09, дефектов нет.** Пять профилей (1440 / 1045 / 768 / 375 с тач-эмуляцией
/ 360): горизонтальной прокрутки нет нигде, рамкой не обрезано, ошибок консоли по нашим файлам 0.
Покупательский путь целиком: библиотека → просмотр через лупу → «Выбрать этот принт» → принт
на макете, **цена 550 → 1550 ₽**; смена стороны и цвета цену не сбрасывают. На телефоне окно
просмотра 351 px при экране 375, лупа видна без наведения, стрелки внутри экрана. **Ни одной
кликабельной цели мельче 32 px** (у конструктора формы на телефоне такая нашлась — см. его файл).

⚠️ Попап сайта «Ваш город Москва?» на телефоне всплывает поверх конструктора и закрывает середину
экрана. Это элемент САЙТА, не наш, но покупателю мешает ровно при знакомстве с конструктором.

🔴 Осталось: отправить `docs/soobshchenie-klientu-2026-09-10-prosmotr-printa.md` и выставить
5 000 ₽ отдельной строкой.

📌 Все замеры, ловушки и обоснования — `docs/razbor-prosmotr-printa-2026-09-10.md`.

## 🟢 13.09 — АДМИНКА: плотности, фасоны и скрытие кнопок (вариант А). ВЫЛОЖЕНО

Комментарии клиента 12.09: «в админке нужны настройки этих полей… нужно настроить плотности
футболок» · «Короткий рукав поменять на "Базовая"» · «внизу добавить третью кнопку
"Длинный рукав"» · «дать возможность отключать эти кнопки из видимости».

Клиент предлагал альтернативу — сразу завести категорию WooCommerce и подтягивать футболки
с атрибутами. **Выбран вариант А (настройки).** Причина: связи с Woo в проекте нет ни строчки,
а у футболок вдобавок нет пути в корзину — категория сама по себе задачу не решает и тянет
за собой отдельный этап вместе с кассой.

Сделано: новая вкладка админки **«Плотности и фасоны»** (граммовка, подпись, цена по каждому
фасону, галочка «Скрыть»; подписи и видимость фасонов) · `AdminOverrides` применяет `densities`,
`prices.form` и `formTypes`, `visibleTypes`/`visibleDensities` отдают видимое покупателю ·
`typeLabel` «Короткий рукав» → **«Базовая»** в конфиге (админка и тест `OrderBuilder` ждали
«Базовую» и раньше — расходился только конфиг) · третий фасон `long` в белом списке PHP и
в `select`. Тесты **151 → 164**.

⚠️ **Скрыть ВСЁ нельзя:** при полном скрытии показываем список как есть — иначе покупатель
остался бы с пустым переключателем.
⚠️ **Кнопка «Длинный рукав» появится у покупателя только когда в каталоге будет изделие этого
фасона с картинкой.** Мокапов длинного рукава НЕТ (`assets/mockups-final/` — только `base_*`
и `over_*`, 12 файлов). От клиента нужны 6 изображений: 3 цвета × 2 стороны.
⚠️ Попутно закрыта мина: сохранение цен печати переписывало раздел `prices` ЦЕЛИКОМ и обнулило
бы цены изделия. Теперь `prices.form` переносится (`$prev_form`).
⚠️ На боевом `admin.json` был пустым `{}` — вкладка рисовала пустые поля цен. Исправлено:
при отсутствии сохранённых цен читаем действующую матрицу из конфига конструктора.

**Замер живого 13.09:** вкладок 4, строк плотностей 7 (4 + 3 пустых), ценовых полей 21,
фасонов 3, ошибок PHP нет · цены в форме 550/600/700/850, у «Длинного рукава» пусто (изделий
ещё нет) · сохранение проверено: в `admin.json` легли `densities` и `formTypes`, цена
в конструкторе осталась 550 ₽ · у покупателя фасоны «Базовая»/«Оверсайз», «Короткого рукава»
больше нет · просмотр принта от 10.09 не сломан (82 плитки, 82 лупы, Esc гасит только превью).

Выложено 10 файлов (9 JS/конфиг + PHP-плагин), сверка SHA-256 сошлась, сайт после заливки PHP
проверен на пяти адресах — все 200. Токен `20260910a` → **`20260913a`**, рамка на 3686 подтянута.
Бэкап боевого PHP — `C:/tmp/jetron-tshirt-admin.php.live-before`.

## 🟢 13.09 — ПРИЁМ ЗАКАЗА: покупатель может купить футболку. ВЫЛОЖЕНО

До этого кнопка «Оформить заказ» существовала, но обработчиков на ней не было ни одного —
покупатель собирал футболку, жал кнопку и оставался на той же странице. Заказ не уходил никуда.

Сделано по образцу конструктора ФОРМЫ (`showOrder` + `jetron-constructor.php` + `jetron-orders.php`):
- `src/js/tshirt/OrderSpec.js` — спецификация для сервера: исходные данные (фасон, плотность,
  возраст, сантиметры принтов, число надписей, количество), **без цен**;
- `mockupDataURL()` — макет вынесен из `downloadMockup`, тот же холст уходит в заказ картинкой;
- модалка подтверждения с количеством, скрытая форма POST в `?add-to-cart=<товар>`;
- **новый mu-plugin `deploy/jetron-tshirt-orders.php`**: скрытый товар + `tshirt/woo.json`,
  приём POST, сохранение макета в `uploads/jetron-tshirt-orders/`, **пересчёт цены НА СЕРВЕРЕ**
  (зеркало `OrderBuilder.js`: база фасон×плотность + ступенчатая печать + надписи со скидкой),
  мета заказа и пометка расхождения.

⚠️ **Сумма из браузера ценой не становится** — едет полем `tshirt_total` только для сверки.
⚠️ **Метод нанесения сервер выводит САМ** из состава: иначе к дорогой печати прислали бы дешёвый
метод. ⚠️ Прайсы сервер берёт из конфига + `admin.json`, поэтому настройки владельца из вкладки
«Плотности и фасоны» действуют и на цену заказа.
⚠️ Картинки в спецификацию не кладём: принты почти всегда data-URL (`PrintEditor` обрезает поля
и перезаписывает `src`), base64 раздул бы POST. Менеджер видит их в макете.

☠️ **Грабля, стоившая замера: скрытому товару нужны ДВА фильтра.** `woocommerce_is_purchasable`
делает его покупаемым, но саму проверку добавления он не проходит — нужен ещё
`woocommerce_add_to_cart_validation`. Симптом обманчив: товар создан и опубликован, `add-to-cart`
отвечает 200, ошибок нет, а корзина пуста. В плагине формы стоят оба (`jetron_force_valid`
и `jetron_force_purchasable`).

**Замер живого боевого end-to-end:** `tshirt/woo.json` создан автоматически (`productId 5648`) ·
из конструктора цена 1550 ₽, количество 2 → в корзине строка 1550 ₽, сумма **3100 ₽** ·
цена НЕ равна цене товара-заглушки (600 ₽), значит пересчёт сервера отработал · макет приложен
картинкой · «Конфигурация» видна в корзине · тестовые позиции из корзины убраны.

Токен `20260913a` → **`20260913b`**, рамка на 3686 подтянута. Тесты **164 → 176**.

## 📄 Инструкция для клиента

`docs/Инструкция-конструктор-футболок-2026-09-13.md` и `.docx` (собран `python-docx`, 11 заголовков,
таблица вкладок, списки — проверено чтением файла). Покрывает: четыре вкладки админки, настройку
плотностей и цен, подписи и скрытие фасонов, что нужно для «Длинного рукава» (6 фотографий),
как приходят заказы и что в них видно, библиотеку принтов с просмотром, редактор зон.
⚠️ **Запись расходится, проверять глазами.** Здесь стояло «клиенту не отправлено», но шапка
«ПЕРЕДАЧА 13.09» и коммит `8d21e1c` («инструкция по футболкам отправлена клиенту 13.09»)
говорят, что отправлена. По времени правды больше у шапки и коммита. **До повторной отправки
Андрею открыть «Отправленные» и посмотреть**, иначе клиент получит дубль.

⚠️ Pandoc для docx не использовать — в соседнем проекте он клал строки друг на друга.
Рабочий путь: `python-docx` (конвертер лежал в `scratchpad/md2docx_ts.py`, при нужде пересобрать).
⚠️ `docs/` в `.gitignore`, поэтому инструкция живёт ТОЛЬКО на этом диске, в репозиторий не едет.

## Открытые хвосты (живые)
1. **Векторизация принта «РУКИ ВВЕРХ! THE BEST»** — отдельный платный разовый заказ того же
   клиента, файлы готовы и проверены замером (`docs/vector-rv/`). Ждёт двух слов клиента:
   **ширина печати в см** (сейчас `PRINT_W_CM = 25.0`) и **цена, рекомендовано 3 000 ₽**.
   Текст готов в `docs/vector-rv/otklik-klientu.md`, **не отправлен**. Как это сделано и
   какие там грабли — раздел «ПЕРЕДАЧА 17.08» в HISTORY.
   ⚠️ Сказано владельцу, НЕ клиенту: «Руки Вверх!» действующий товарный знак.
2. **КП по админке футболок не отправлено** (обсуждалось 18 000 ₽ отдельно / 30 000 ₽ за обе).
3. **Сантиметры детской зоны**: рамка тянется, а в заказ всё равно уходит 30×40. Спросить,
   если у клиента детская печать реально другого размера.
4. **Смена цвета футболки под уже положенный принт**: поведение не меняли, принт остаётся
   без предупреждения. Клиенту предложен вариант «оставить и показать заметку», ответа нет.
5. Мокапы: пилот переделки в `docs/mockup-pilot/` (Nano Banana Pro, IoU 98.7%). Решения нет.
6. Векторизация загружаемых покупателем JPEG→PNG — нужна ли услуга и по какой цене.
7. Пояснения к цветам: текст БЕЛОГО дословно от клиента, чёрный и слоновая кость написаны
   нами и ждут подтверждения. ⚠️ `keepNotes()` в `AdminOverrides` подставляет пояснение из
   базового конфига по `id`, иначе правка цвета через админку молча стирала бы текст.

## 🧰 Грабли инструментов (общие, стоили времени не по разу)
- **tdd-guard: у встроенного `node --test` репортера нет.** Гонять ТОЛЬКО через мост
  `node C:/Projects/.claude/tdd-guard/node-report.mjs tests/X.test.js`: он пишет
  `.claude/tdd-guard/data/test.json` и повторяет код возврата. Голый `node --test` оставляет
  сторожу устаревшие данные, и тот блокирует каждую правку. Мост печатает только сводку,
  упавший тест смотреть в `data/test.json`.
- **Отладочная ручка в браузере — `window.__tshirtApp`** (`main.browser.js:91`). `window.app`
  это DOM-узел `#app` и ловушка: `Object.keys` даёт `[]`, `state` = null.
- **Дескрипторы слоёв содержат `src` в виде data-URL.** `JSON.stringify` целого дескриптора
  выбивает лимит ответа (ловил 461 147 знаков). Фильтровать длинные строки до вывода.
- `location.reload()` внутри `preview_eval` убивает сам eval («Inspected target navigated»).
  Перезагрузка и замер — двумя отдельными вызовами.
- Живую правку поля ввода проверять НЕ значением, а тождеством узла
  (`document.querySelector(sel) === input`) и `document.activeElement`.
- Проверка синтаксиса через `new Function(код без import)` НЕ ловит дублирующийся импорт —
  он однажды уронил весь конструктор при зелёной проверке. Проверять загрузкой страницы.
- `Number(null)` даёт 0, а не NaN; `Number([1400])` даёт 1400. Пустая настройка молча
  превращалась в самое узкое поле.
- Замер тач-целей на 375px в браузере с МЫШЬЮ бессмысленен: `@media (pointer: coarse)`
  не активируется. Проверять через `matchMedia('(pointer: coarse)').matches`.
- Находимость элемента проверяется ГЕОМЕТРИЕЙ относительно обрезающего предка, а не
  доставкой событий: синтетика не видит `overflow: hidden` у родителя.
- Расшифровка видео и голосовых: `faster-whisper` (small, language=ru), ffmpeg из
  `imageio_ffmpeg`. Вывод в файл (кириллица в консоли ломается, `PYTHONIOENCODING=utf-8`).
- `cd` сбрасывается между вызовами Bash, пути абсолютные; для curl `127.0.0.1`, не `localhost`;
  Edit требует предварительного Read того же файла.
- **Мост tdd-guard берёт ФАЙЛЫ, а не каталог**: `node .../node-report.mjs tests/` даёт
  `MODULE_NOT_FOUND`. Разворачивать глоб: `$(ls tests/*.test.js | tr '\n' ' ')`.
- **`javascript_tool` не ждёт промисов** — возвращает `undefined` или `{}`. Всегда в два
  вызова: первый кладёт результат в `window.__out` внутри `.then()`, второй читает
  `JSON.stringify(window.__out)`.
- ⚠️ **В НЕАКТИВНОЙ вкладке таймеры душатся до тика в минуту.** Серия 12×700 мс должна занять
  8 с, а не завершилась и через четыре вызова инструмента: выглядит как зависшая страница,
  но это троттлинг фона. Длинные серии проб делать отдельными вызовами, не в `setTimeout`.
- **Классы берём из кода, а не из головы**: кружок с галочкой это `.text-badge__dot`,
  а `.color-opt` в проекте нет вовсе — селектор-догадка молча вернул «узел не найден».
- **Ответ инструмента режется на строках с query string**: вернул адрес рамки с `?v=` —
  получил `[BLOCKED: Cookie/query string data]`. Возвращать только производные факты
  (числа, длины, булевы), а не сырые URL.
- **`file_upload` больше не принимает путь на диске**, требует содержимое в параметре
  `files`. Значит 200+ КБ base64 попадут в контекст: такую заливку делать СУБАГЕНТОМ.
- **Класс окна библиотеки — `libm`** (`libm__card/__cat/__cell/__thumb/__close`). Селектор
  `[class*="library"]` цепляет ЧУЖОЙ статичный элемент 74 px и даёт ложное «position: static».
- `document.body` на странице wp-admin бывает `null` — писать защитно, иначе `TypeError`
  скрывает настоящую причину (например, что открыта форма входа).

## ⚠️ Правило работы с этим клиентом
**Перед утверждением «у клиента сломано X» открыть конструктор глазами и убедиться, что
покупатель это видит.** Дважды ошибался обратным порядком (в соседнем проекте формы),
клиент оба раза оказался прав. Он же 01.08: «давайте уже заканчивать… только вот сами
протестируйте, а то мы часто по кругу ходим». Поэтому каждый пункт закрывается ЗАМЕРОМ.
