# tshirt-constructor — конструктор футболок

Статус-хендофф. Обновлено: 2026-07-24. Self-contained контекст для любой сессии/аккаунта.

## Что это
Онлайн-конструктор футболок (vanilla JS, без сборки): выбор типа (базовая/оверсайз), плотности, цвета, стороны (перёд/спина), загрузка принта в рамку с ценой по размеру в см, текст, итог заказа.

## Дизайн (v2, 24.07) — единая система с jetron
Оба конструктора будут жить на jetronsport.ru в соседних вкладках, поэтому дизайн-система общая:
Oswald (заголовки/цена) + Manrope (интерфейс) из `assets/fonts/`, тёплая кремовая гамма (`--bg #fbf1db`,
`--stage-bg #fdf9ef`), синий акцент `#2f6fe0`, тёмно-синий CTA `#14294c`. Токены в `:root` в `src/css/app.css`.
Раскладка: `#app` = сцена + панель 372px, как в `jetron-constructor/src/css/stand.css`.

Ключевые решения раскладки:
- Сцена показывает **обе стороны разом** (как jetron). Клик по карточке `.canvas-wrap` делает сторону
  активной (`state.side`) — туда ложатся принт и надпись. По одному `PrintEditor` на сторону (`this.editors`).
- Цвет и фасон — **под макетом** (`#colorpick`), карточками с живым превью, а не списком в панели.
- Панель = карточки-секции «Изделие / Принт / Надпись» + липкий `.price-box` с крупной ценой и CTA.
- Таблица размеров свёрнута в `.sizes-toggle`.

⚠️ НЕ ПУТАТЬ с `jetron-constructor` (конструктор ФУТБОЛЬНОЙ ФОРМЫ, линейки Champion/Legend). Тот же заказчик (jetronsport.ru), но это отдельный продукт и отдельное репо. Линеек здесь НЕТ: каждая футболка будет привязана к конкретной карточке товара WooCommerce.

## Где что
- Демо: https://andrrreyiv.github.io/tshirt-constructor/ (GitHub Pages, деплой = push в main)
- Конфиг и ценовое ядро: `src/config/tshirt-mock-config.json` — решения заказчика записаны в `_note`-полях с датами
- Код: `src/js/tshirt/{StepPrice,OrderBuilder,PrintEditor,...}.js`, UI: `src/js/ui/TshirtApp.js`
- Тесты: `node --test tests/*.test.js` (именно с глобом; npm-скрипта test нет). Сейчас 33/33
- `docs/` в .gitignore СОЗНАТЕЛЬНО (внутренние материалы) — журнал вопросов `docs/ОТКРЫТЫЕ-ВОПРОСЫ.md` живёт только локально

## Ценообразование (состояние 23.07)
- ГЛАВНОЕ: на боевом сайте цена САМОГО ИЗДЕЛИЯ подтягивается из карточки товара. Матрица в конфиге (base 180=600 / oversize 200=800 / 240=1000) — демо-заглушка. Детская = взрослая.
- Печать: ступенчатая (rule=min-containing), два метода с переключателем в UI — DTF (до 40×40=1050, 40×50=1250) и плёнка (до 30×40=700, 40×50=800). Возраст влияет на рамку: adult 40×50, child 30×40.
- Текст: standalone 500 (placeholder, ждёт подтверждения), при принте скидка 50% (не показывать явно).
- Цвета старта: белый, чёрный, слоновая кость (ivory подтверждена 23.07, pending снят).

## Замечания клиента 28.07 — НЕ реализованы (подробно: docs/КОММЕНТАРИИ-КЛИЕНТА-2026-07-28.md)
1. **Раскладка не по его макету.** Нужно: слева ОДНА крупная футболка (активная сторона), справа
   маленькие превью перёд/спина БЕЗ рамок и подписей размера. Сейчас обе стороны равного размера рядом.
2. **Зоны печати груди и спины разного размера** — должны быть одинаково большие.
3. **Мокап заменить** на присланный клиентом (на сером фоне); текущий кривой снизу и низкого качества.
4. **Библиотеку принтов вынести в отдельное модальное окно** с категориями слева и сеткой картинок
   справа (сейчас список в панели → при росте числа принтов «полотенце»). Референс — конструктор
   конкурента: **votprikid.ru → /designer**. ⚠️ Конструктор у них НЕ свой — встроенный сторонний
   виджет `https://cosuv.ru/app/3786` (iframe). Их окно принтов (проверено вживую): сверху зона
   «Выберите файл / перетяните файл сюда», слева прокручиваемый список категорий (Все картинки,
   14 февраля, 23 февраля, Военные, Для беременных, Для детей, Для рыбалки, Женские, Мужские,
   Животные, Свадьба…), справа сетка превью. Поиска у них нет. Подробно: docs/КОММЕНТАРИИ-КЛИЕНТА-2026-07-28.md.
5. **Принт не растягивается до краёв рамки** — проверить ограничения масштаба в PrintEditor.
По футбольной форме клиент сказал «вроде всё ок» — там работа на его стороне.

## Ответы клиента 28.07 (голос 17:30) — часть вопросов ЗАКРЫТА
- **Качество оверсайза — ОТЛОЖЕНО клиентом.** «Оверсайз 80% (736px), поэтому выглядит хуже. Пока
  оставьте футболки как есть, потом будем разбираться. Если будет в админке регулировка, я сам
  подберу футболки.» Значит: мокапы НЕ переделываем, дизайнера не ждём, вопрос уходит в админку.
- **Оверсайз чёрная спина — ЗАКРЫТО.** Настоящего фото нет: «Если перекрасили, то хорошо».
  Перекраска из белой принята как штатное решение (U13 снят).
- **Библиотека принтов — принято.** «Согласен, список в панели… а мы вроде переделали» (окно сделано 28.07).
- **Цена надписи без принта — ЗАКРЫТО: 500 руб.** «Оставляйте 500, я потом подрегулирую, как мне надо.»
  То есть значение уже не placeholder, но нужна регулировка из админки.
- **Раскладка по макету — подтверждена**, ждёт реализации: «слева одна крупная, это да».
⚠️ Клиент дважды намекнул на админку для футболок (подбор мокапов, регулировка цен) — КП по ней
ещё не отправлено, обсуждалась цена 18 000 отдельно или 30 000 за обе админки.

## Размещение на сайте — ОТВЕТ клиента 28.07 (голос 18:40)
Конструктор футболок делается под категорию, которой ЕЩЁ НЕТ: «футболки с футбольными принтами»,
отдельная хлопковая футболка (не форма). Поэтому:
- **Сейчас:** оба конструктора положить в бургер-меню сверху рядом — «Конструктор формы»
  (или «Конструктор футбольной формы») и «Конструктор футболок». «Пускай там висят, там и будем тестить.»
- **Потом:** когда обновят страницу, кнопки расставят по разделам, появится страница про нанесение,
  и конструктор футболок переедет в категорию «футболки с принтами».
- Где именно сейчас — клиенту НЕ важно: «сейчас не важно, где он».
⚠️ Для пункта меню конструктор футболок надо где-то разместить: на боевом его НЕТ (только демо
GitHub Pages). Либо выкладывать на jetronsport.ru, либо временно вести пункт на демо-адрес.
⚠️ На сайте ДВА меню (десктоп + мобильное), пункты добавлять в оба.

## Боевой сайт jetronsport.ru — ПЕРЕНЕСЁН 29.07
- Конструктор: `https://jetronsport.ru/tshirt/` (файлы репозитория один в один, деплой через WP File Manager).
- Страница для покупателя: `/onlajn-konstruktor-futbolok/` (ID 3686), iframe `id="jetronConstructor"`
  ⚠️ id менять НЕЛЬЗЯ: скрипт авто-высоты на сайте ищет именно его. Плюс блок full-bleed CSS,
  иначе на телефоне 66px съедают `.page-content` (25px) и `.container` (8px).
- Меню: пункт «Конструктор футболок» в обоих меню (десктоп ID 3, мобильное ID 55) рядом
  с «Конструктор формы» — как просил клиент (кнопка в бургер-меню). Проверено вживую на 375px.
- Кэш: токен `?v=` в `index.html` (сейчас 20260729b) бампать при каждом деплое CSS/JS.

## Админка клиента (mu-plugin, 29.07)
- Файл: `deploy/jetron-tshirt-admin.php` → на сервере `wp-content/mu-plugins/jetron-tshirt-admin.php`.
- Пункт меню WP «Конструктор футболок», три вкладки: цены печати и надписи · библиотека принтов ·
  изделия и цвета. Пишет `tshirt/admin.json` и `tshirt/prints.json`.
- Наложение настроек: `src/js/tshirt/AdminOverrides.js` (11 тестов). Битый или пустой раздел
  игнорируется — конструктор молча остаётся на базовом конфиге. Цена ИЗДЕЛИЯ в админке не
  настраивается: она из карточки товара.
- ⚠️ Грабли из конструктора формы: форма сброса раздела рендерится ТОЛЬКО после `</form>`
  основной формы. Вложенные формы браузер схлопывает, и сохранение молча превращается в сброс.
- Проверка PHP без локального php: залить исходник как `tshirt/_lint-*.php`, запросить (валидный
  файл отдаёт пустой 200, потому что первым делом `exit` без ABSPATH), затем удалить.
- Проверено вживую 29.07: сохранение цен, валидация пустых ступеней, загрузка принта и мокапа
  (транслит имён файлов), удаление, сброс всех разделов. Тестовые данные и файлы убраны.

## Проверка 29.07 и добор пунктов КП (коммит 1bb4dae)
Дописано то, чего не было, но что обещано клиенту:
- **переименование категорий** принтов (меняется подпись, папка с картинками остаётся);
- **порядок категорий** стрелками «выше/ниже» — этот список видит покупатель слева;
- **загрузка картинок пачкой** (`print_file[]`, multiple): битые файлы пропускаются, в уведомлении перечисляются причины.
Проверено вживую на боевом: переименование, перестановка, край списка, пачка из 3 файлов (транслит имён), конструктор подхватил и новый порядок, и новое имя, и 5 картинок в категории. После проверки библиотека сброшена к исходной, тестовые файлы удалены.

## Открытые хвосты
1. Цена текста без принта — placeholder 500, подтвердить (единственный незакрытый параметр ценового ядра).
2. Мокапы: пилот переделки в `docs/mockup-pilot/` (Nano Banana Pro через OpenRouter, 2048px, прозрачный фон,
   силуэт сохранён — IoU 98.7%). Ждём решение заказчика по качеству и по тому, что мокап не его реальное изделие.
3. Векторизация JPEG→PNG загружаемых принтов — нужна ли услуга и по какой цене.
4. WebP во входных форматах (по умолчанию включён), варёнка (нужен образец текстуры).
5. Интеграция на jetronsport.ru — платформа подтверждена (WooCommerce), доступ и встраивание позже.

Закрыто 24.07: плотности тянутся из карточки товара, отдельный список для админки не нужен.
Сроки/бюджет из вопросов заказчику сняты — определяются на нашей стороне.

## Грабли
- Edit/Write-инструменты иногда падают с `Not logged in` на файлах проекта → обход: node-скрипт точечных замен через Bash (однострочные якоря + `includes`-проверка).
- Кириллический вывод в консоли ломается → `PYTHONIOENCODING=utf-8` и/или вывод в файл.
