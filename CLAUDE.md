# tshirt-constructor — конструктор футболок

Статус-хендофф. Обновлено: 2026-08-25. Self-contained контекст для любой сессии/аккаунта.

📚 **Вся история работ 23.07-17.08 лежит побайтово в `docs/HISTORY-tshirt-2026-08-25.md`**
(полная копия прежнего файла, 88 421 байт, 775 строк, 28 разделов, ничего не выброшено).
Нужна подробность по закрытой работе — она там, слово в слово. Здесь только живое.
⚠️ Папка `docs/` в .gitignore сознательно, поэтому файл истории лежит ЛОКАЛЬНО на этом
компе и в репозиторий не попадает — как и все прочие внутренние материалы проекта.

### Правило ведения этого файла
Закрытая работа сюда не пишется. Сделали и проверили — строка о результате, подробности
в HISTORY. Как только в разделе хочется написать третий абзац про уже закрытое, он едет
в HISTORY целиком. Прежний файл дорос до 88 КБ и съедал ~25k токенов в каждой сессии
до первого слова работы, отсюда постоянные автосжатия.

## Что это
Онлайн-конструктор футболок (vanilla JS, без сборки): тип (короткий рукав / оверсайз),
плотность, цвет, сторона (перёд/спина), принт из библиотеки или свой файл, надпись,
итог заказа. Сдан и принят клиентом 02.08.2026, дальше идут доработки по его замечаниям.

⚠️ **НЕ ПУТАТЬ с `jetron-constructor`** — это конструктор футбольной ФОРМЫ (линейки
Champion/Legend). Тот же заказчик (jetronsport.ru), но отдельный продукт и отдельное репо.

## Где что
- Демо: https://andrrreyiv.github.io/tshirt-constructor/ (GitHub Pages, деплой = push в main)
- Боевой: `https://jetronsport.ru/tshirt/`, страница покупателя `/onlajn-konstruktor-futbolok/` (ID **3686**)
- Конфиг и ценовое ядро: `src/config/tshirt-mock-config.json` (решения заказчика в `_note`-полях с датами)
- Код: `src/js/tshirt/*.js`, UI: `src/js/ui/TshirtApp.js`, стили: `src/css/app.css`
- Админка: `deploy/jetron-tshirt-admin.php` → `wp-content/mu-plugins/` на сервере
- Тесты: **137/137**. Прогон только через мост tdd-guard (см. ниже), не голым `node --test`

## ⚠️ ГЛАВНОЕ ПРАВИЛО ДЕПЛОЯ: версия в адресе iframe
Страница 3686 встраивает конструктор как `<iframe id="jetronConstructor" src=".../tshirt/?v=ТОКЕН">`.
**Залить файлы НЕДОСТАТОЧНО.** Не сменил ТОКЕН в адресе — вернувшийся посетитель получает
из кеша и старый `index.html`, и все модули с тем же `?v=`, то есть ВСЮ старую сборку.
Отсюда рождается «я обновляю, а ничего не меняется».
- ⚠️ **id рамки менять НЕЛЬЗЯ**: скрипт авто-высоты сайта ищет именно `jetronConstructor`.
- Токен правится через REST `POST /wp-json/wp/v2/pages/3686` (текстового поля в редакторе
  нет, страница собрана билдером, токен лежит в `content.raw`). Нонс: `GET /wp-admin/admin-ajax.php?action=rest-nonce`
  — обычный nonce со страницы отдаёт `rest_cookie_invalid_nonce`.
- Перед патчем убедиться, что вхождение токена РОВНО одно и длина контента не меняется.
- Токен бампать и для ЛОКАЛЬНОЙ проверки: ESM кешируется по URL, `location.reload()` не лечит.
- **Текущий токен сборки: `20260825b`**, на странице 3686 поднят 25.08 (REST, статус 200,
  вхождение одно, длина контента 1230 не изменилась).
  ⚠️ Прежний живой токен на 3686 был **`20260801d`**, а не `20260825a`, как раньше врал этот
  файл. Поймано только тем, что перед патчем считали вхождения, а не меняли вслепую.
  Правило: считать `content.raw`, а не верить записи о токене.
- ⚠️ **Заливка идёт ТОЛЬКО из живого Chrome владельца** через расширение Claude in Chrome.
  `chrome-devtools` MCP не подходит: свой чистый профиль, сессии WP нет, антибот отдаёт капчу.

### Механика заливки (WP File Manager, elFinder)
- 1R AntiBot режет curl, работать через Claude in Chrome. PUT и DELETE у REST антибот тоже
  режет (отдаёт HTML со статусом 200), POST проходит.
- Текст: `cmd=put`, **без** `encoding=scheme`, ОДИН файл за вызов (батч отклоняется классификатором).
- `cmd=put` пишет только в СУЩЕСТВУЮЩИЙ файл: под новый сначала `cmd=mkfile`.
- Бинарники (шрифты, картинки) только `cmd=upload` с FormData и `overwrite=1`, target = хеш КАТАЛОГА.
- Проверять НАЛИЧИЕ в листинге `cmd=open`, а не HTTP-статусом: антибот отдаёт свою HTML-страницу
  (~17 КБ) со статусом 200 вместо 404.
- Сверка деплоя надёжнее всего по SHA-256 через `crypto.subtle` в браузере.
  ⚠️ Правило «первый байт 0 = порча» верно только для текстовых файлов: TTF законно
  начинается с `0x00` (сигнатура sfnt).

## Настройки на сервере (их правит клиент, кодом не трогать)
`tshirt/admin.json` (цены печати и надписи), `tshirt/prints.json` (библиотека и тон),
`tshirt/zones.json` (зоны печати, ключ `child` — отдельная детская коробка),
`tshirt/crops.json` (кадр мокапа у каждой модели).
Наложение — `src/js/tshirt/AdminOverrides.js`: битый или пустой раздел игнорируется,
конструктор молча остаётся на базовом конфиге.
Редактор зон и кадра: `/tshirt/?zones=edit` (к КОРНЮ: `serve` роняет query на `/index.html`).
Гость панель увидит, но сохранить не сможет: экшены требуют `manage_options` и nonce.

## Ценообразование
- Цена САМОГО ИЗДЕЛИЯ на боевом тянется из карточки товара WooCommerce. Матрица в конфиге
  (base 180=600 / oversize 200=800 / 240=1000) — демо-заглушка.
- Печать ступенчатая (`rule=min-containing`), два метода: DTF (40×40=1050, 40×50=1250)
  и плёнка (30×40=700, 40×50=800). Возраст задаёт рамку: adult 40×50, child 30×40.
- Текст: standalone **500 ₽** (подтверждено 28.07), при принте скидка 50%, явно не показывать.
- ⚠️ **Пути в корзину у конструктора футболок НЕТ** (`OrderBuilder.js`: «за блокером»).
  Когда кассу будут делать — НЕ подключать сумму из браузера к `set_price`. Готовый
  правильный образец: `jetron-constructor/deploy/jetron-orders.php`, копировать оттуда.

## 📌 ПЕРЕДАЧА 25.08: ЧЕТЫРЕ ЗАМЕЧАНИЯ КЛИЕНТА — ЗАКРЫТЫ И ВЫЛОЖЕНЫ НА БОЕВОЙ

Клиент прислал видео и голосовое. Расшифровка: `docs/voice-client-2026-08-25.txt`.
Тесты 124 → **126/126**. **Все 11 файлов на боевом, токен 3686 поднят до `20260825b`.**

### 1. Принт сам сжимался в минимум после изменения размера
Проверено замером в браузере: тянем принт до 0.3846×0.47, дальше три цикла клика —
геометрия каждый раз та же. То же на надписи (1×0.3243). Схлопывания нет.
⚠️ Слушатели жеста висят на РУЧКЕ, а не на window: `PrintEditor._beginGesture` делает
`setPointerCapture` и вешает `pointermove`/`pointerup` на `.pf-print__handle`.
Синтетический драг слать туда же, иначе «ничего не происходит» и это ложная тревога.

### 2. Смена шрифта не меняла уже набранную надпись
Закрыто. ⚠️ Ширина элемента между шрифтами НЕ меняется, и это не признак отката на
дефолт: элемент это коробка слоя, а не строка глифов. Проверять `document.fonts` и
canvas `measureText`: все пять начертаний `loaded`, «Маша» 48px даёт 141.36 (russoone) /
109.73 (oswald) / 158.88 (prostoone) против 122.25 у serif — три разных живых шрифта.
`fontId` доходит до дескриптора слоя, `fontName` — до заказа.

### 3. Метод нанесения теперь навязывается, а не выбирается
Клиент: «если человек просто надпись выбрал, то там делаем плёнкой… кнопку плёнкой, и всё,
ничем поменять не может. Если он выбрал принт или загрузил картинку, то там делаем только
DTF, там кнопку плёнка убираем». Причина у него денежная: «она плёнкой дешевле, они будут
тыкать плёнкой, а потом лишнее объяснять им, что это на самом деле плёнкой не получится».
- `src/js/tshirt/PrintMethod.js`: чистая `forcedMethod({hasPrint, hasText})` → `dtf` / `film` / `null`.
- В панели остаётся РОВНО ОДНА кнопка, а не две с одной активной. На пустой футболке
  поля метода нет вовсе. Есть принт (хоть с надписью) — только DTF, и `state.printMethod`
  принудительно переводится в `dtf`, иначе цена осталась бы плёночной.
- ⚠️ **Дыру нашли глазами, тесты её не видели.** Покупатель печатал «Маша», цена
  становилась плёночной, а строки «Плёнкой» на экране НЕ БЫЛО до первого клика по другому
  полю. Причина в тихом режиме: набор надписи нарочно не пересобирает панель, иначе слетает
  фокус с поля ввода. Лечение: поле метода живёт в постоянном гнезде `.method-slot`,
  которое обновляет `refreshMethodField()` из `updatePrice()` — единственного пути, по
  которому тихий режим ходит. Замер после правки: с ПЕРВОГО символа гнездо показывает
  «Плёнкой», цена 1050 ₽, фокус на месте, узел поля ввода не пересоздаётся.
- ⚠️ Побочный эффект, пойман замером: постоянное гнездо сломало `.field:last-child`,
  под последним полем осталось 16px пустоты. Лечится `.method-slot:empty { margin-top: -16px }`
  (`:has()` не берём, он моложе части браузеров). Замер после: зазор 0.00002px.

### 4. «Ползёт вниз, ползёт до бесконечности» — ДВЕ причины, обе закрыты
Клиент на видео: «Видишь, ползёт вниз, вот этот экран. Он ползёт, ползёт и он ползёт
до бесконечности». Локально без рамки авто-высоты дефект не воспроизводится вообще,
поэтому написан стенд `docs/iframe-harness.html`, буквально повторяющий поведение сайта
(`frame.style.height = высота документа внутри` каждые 100 мс, все замеры в `window.__замеры`).

**Причина A — окно библиотеки принтов.** Было `position: absolute`, а абсолютный элемент
входит в `scrollHeight`: прокрутили вниз → `top` вырос → документ стал выше → сайт увеличил
рамку → следующий кадр сдвинул окно ещё ниже. Лечение: `fixed` (`pinStyle` в `LibraryPanel.js`).
Внутри рамки своей прокрутки нет, `scrollY` всегда 0, поэтому на экране окно встаёт туда же,
но высоту документа больше не кормит.

**Причина B, ГЛАВНАЯ, найдена замером уже ПОСЛЕ правки A.** Страница ползла сама, без всякой
библиотеки, ровно на **16 px за кадр** (замер: 3766 → 3782 → 3798 → 3814 → …). Вся петля
была в одной строке CSS: у `body` стояло `min-height: 100vh`, а внутри iframe `100vh` это
высота САМОЙ РАМКИ. Сайт ставит рамке высоту документа → документ дотягивается min-height
до новой высоты рамки → `documentElement.scrollHeight` всегда на 16px больше `body` →
следующий кадр добавляет ещё 16. Строка удалена; фон не пострадал, потому что у `html`
своего фона нет и фон `body` по правилам CSS красит весь холст.

⚠️ **Стенд сначала мерил не то свойство и потому ничего не доказывал.** `body.scrollHeight`
абсолютный оверлей НЕ ВИДИТ вовсе — он остаётся 1977 даже при `top: 3000px`; растёт только
`documentElement.scrollHeight` (1993 → 3430). Стенд переведён на максимум из двух.
⚠️ И второе, тоже замером: `documentElement.scrollHeight` внутри рамки **не бывает меньше
высоты самой рамки**. Поэтому скрипт, меряющий его, рамку умеет только увеличивать. Пока
внутри есть хоть какая-то добавка сверх высоты окна — рост бесконечен; когда добавки нет,
получается неподвижная точка (замер: `html.scrollHeight` = `innerHeight` = 2139 ровно).

**Проверено в обе стороны, это главное:** после правки — 2011 px, ноль роста за 22 замера
в покое и ещё за 31 с открытой библиотекой при прокрученной странице, окно библиотеки
ложится ровно в экран (0…730 при экране 730). Контрольная проверка: временно вернули
`min-height: 100vh` — рост немедленно возобновился, 2027 → 2043 → 2059 → … → 2139,
+16 за замер, монотонно. Убрали — снова ноль. Тест-сторож `tests/OverlayPin.test.js`
не пускает `vh` обратно в высоту `body`.
⚠️ В превью-браузере `requestAnimationFrame` НЕ ТИКАЕТ вовсе (замер: 0 кадров), поэтому
привязка окна к прокрутке там выглядит сломанной. Это артефакт окружения: ручной
`dispatchEvent(new Event('scroll'))` мгновенно даёт расчётные значения. Не чинить.

### ✅ Выкладка 25.08 выполнена и проверена НА БОЕВОМ, а не «должно работать»
Владелец разрешил («выложи на боевой сайт клиента») и открыл админку. Залито 11 файлов:
`index.html`, `src/css/app.css`, `src/js/core/OrderSummary.js`, `src/js/main.browser.js`,
`src/js/tshirt/AdminOverrides.js`, `src/js/tshirt/LayerManager.js`, `src/js/tshirt/LibraryPanel.js`,
`src/js/tshirt/PrintEditor.js`, `src/js/tshirt/zone-editor.browser.js`, `src/js/ui/TshirtApp.js`
и НОВЫЙ `src/js/tshirt/PrintMethod.js` (774 б, его на сервере не было — заведён `cmd=upload`).
Тесты и `CLAUDE.md` на боевой не идут.

**Сверка не по отчёту исполнителя, а своими руками:** все 11 файлов заново скачаны с
публичных адресов с антикешем, посчитан SHA-256 через `crypto.subtle` и сличён с диском →
`{"всего":11,"сошлось":11,"строки":[]}`. Размеры совпали тоже.

**Замеры на живой странице покупателя `/onlajn-konstruktor-futbolok/`:**
- Дефект «ползёт вниз» мёртв: 14 проб за 12 с в покое — высота рамки 1046 неподвижна,
  рост 0. Ещё 11 проб с ОТКРЫТОЙ библиотекой и прокруткой родителя 400↔1000: рамка
  1084 → 1084, внутренний документ 1084 → 1084, рост 0 по обоим.
- Окно библиотеки `position: fixed`, 12 категорий, 17 принтов, высота внутреннего
  документа равна высоте рамки ровно, то есть оверлей в `scrollHeight` не входит.
  ⚠️ Миниатюры на скриншоте выглядели серыми — проверено `naturalWidth`, все 17 живые.
- Метод нанесения глазами покупателя: пустая футболка — поля метода нет вовсе; печатаем
  «Маша» — ровно одна кнопка «Плёнкой», кнопки DTF нет, цена 1050 ₽, фокус на месте,
  узел поля ввода не пересоздан.

### ⚠️ Что осталось по этой передаче
1. **Резервный архив `tshirt-backup-20260825-4e89758cd207079e45b06d5b.zip` лежит в
   `public_html` и скачивается кем угодно по прямой ссылке** (5 976 420 б, вся папка
   `tshirt` до правок — это откат). Угадываемое имя убрано переименованием, старый адрес
   `/tshirt-backup-20260825.zip` теперь отдаёт 403 и не ZIP (проверено).
   🟢 **26.08 владелец разрешил удалить, деплой пересверен заново:** все 11 файлов скачаны
   с боевого с антикешем, SHA-256 через `crypto.subtle` → `{"всего":11,"сошлось":11}`,
   размеры совпали. Архив больше не нужен, откатывать нечего.
   🔴 **Удалить не смог: весь `/wp-*` отдаёт «Access denied.» (165 б) в MCP-вкладке Chrome** —
   и `/wp-admin/`, и `/wp-admin/admin.php?page=wp_file_manager`, и даже `/wp-login.php`.
   При этом публичный `/tshirt/` открывается нормально, то есть режет не сеть, а антибот
   или отсутствие сессии WP именно в этой группе вкладок. Нужен вход владельца в админку
   внутри той же группы вкладок, после этого удаление это один `cmd=rm` в elFinder.
   ⚠️ Проверять удаление листингом `cmd=open`, а не HTTP-статусом.
2. Отчёт клиенту по четырём пунктам не написан и не отправлен.

### 🟢 26.08: работа 25.08 закоммичена и запушена
Была в единственном экземпляре на диске 26 дней. Коммит `e826285` в ветке
`docs/handoff-2026-07-31`, 18 файлов, +970/−750, запушен в origin, расхождения с origin нет.
Тесты перед коммитом прогнаны: **126 pass, 0 fail**.
⚠️ `docs/` целиком в `.gitignore` («внутренние материалы клиента»), поэтому под гитом
НЕ лежат ни `docs/vector-rv/` (векторные файлы принта), ни расшифровки голосовых,
ни HISTORY. Это осознанное решение, но копия у них ровно одна — на этом диске.

**Готовность сборки подтверждена замером 25.08, а не «должно работать»:** тесты 126/126
через мост; дублей импорта нет; локальная страница на токене `20260825b` поднимается,
`window.__tshirtApp` живой (значит модули не побиты), `getComputedStyle(body).minHeight`
= `0px`, гнездо `.method-slot` на месте, фон `html` прозрачный при кремовом `body`.

## 📌 ПЕРЕДАЧА 26.08: ПЯТЬ ЗАМЕЧАНИЙ КЛИЕНТА — ИСПРАВЛЕНЫ И ЗАКРЫТЫ ЗАМЕРОМ

Три скриншота и пять голосовых от 26.08 (`C:/Users/Андрей/Downloads/football/`). Расшифровка
через `faster-whisper`. Тесты 126 → **137/137**, десять новых тестов в четырёх файлах.
🔴 **На боевой НЕ выложено, токен НЕ бампался, отчёт клиенту не написан.**

Все пять пунктов проверены в браузере на локальном стенде, каждый — контролем в обе стороны,
а не «должно работать».

### 1. Миниатюра съедала первую и последнюю букву
Клиент: «На основной футболке написано смирнов, а на миниатюре написано мирно».
Причина: `.sideprev__text` имел `font-size: clamp(6px, 2.2vw, 13px)` — кегль привязан
к ШИРИНЕ ОКНА, а не к коробке слоя. Коробка в миниатюре пропорциональна `fw`/`fh` слоя,
поэтому у мелкой надписи 13px не влезали и `overflow: hidden` срезал текст с ОБЕИХ сторон
(текст центрирован). Лечение в CSS: `container-type: size` у `.sideprev__item` и
`font-size: 60cqh` — тот же закон, по которому кегль считает `PrintEditor._applyTextSize`
(`h * fh * 0.6`). Строка `clamp(...)` оставлена ВЫШЕ как запасная для браузеров без
контейнерных единиц. `cqh`, а не `:has()`: в Firefox контейнерные запросы с 110 (фев 2023),
`:has()` только с 121 (дек 2023).
⚠️ **Замер при полноразмерном слое дефекта НЕ показывает** и едва не дал ложный вывод
«всё и так работает». Ловится только на уменьшенном слое. Контроль на живой странице,
слева прежнее правило, справа новое:
`0.80×0.16` → 13px, 94/94, цело · 14.14px, 94/94, цело
`0.45×0.09` → 13px, нужно **56** при видимых **53**, ОБРЕЗАНО · 7.95px, 53/53, цело
`0.30×0.06` → 13px, нужно **47** при видимых **35**, ОБРЕЗАНО · 5.30px, 35/35, цело

### 2. Названия шрифтов красились в цвет надписи
Клиент: «они стали белыми… Они должны быть всегда чёрными. Независимо от того, что
выбирает человек на футболку». Цвет присваивался в JS дважды: в `fontList()`
(`sample.style.color = this.state.textColor`) и в обработчике `input` пикера, который
ещё и обходил все образцы. Обе строки удалены, цвет задан в CSS: `.font-opt__sample { color: var(--ink) }`.
Замер: при белой и при малиновой надписи все три названия `rgb(27, 27, 27)`.
Контроль — вернули цвет инлайном, названия немедленно стали `rgb(225, 29, 72)`.
⚠️ Шрифтов в конфиге **три** (Russo One, Oswald, Prosto One), а не пять.

### 3. Галочка в зелёном кружке стояла не по центру
Причина: галочка рисовалась повёрнутым на 45° псевдоэлементом `::after` и центровалась
пикселями `left`/`top`. Так центр не поймать вообще: у повёрнутого прямоугольника
габаритная коробка не совпадает с собственной. Псевдоэлемент убран, галочка стала
инлайновым SVG в `background`. Замер: кружок 13×13, `background-position: 50% 50%`,
`background-size: 9px 9px`, `::after` — `content: none`.

### 4. Удаление надписи на футболке не чистило поле ввода
Клиент: «если уж на футболке удаляем слово смирнов, то… и в поле смирнов пусть это слово
тоже удалится», «обратно уже вернуть не можем… оно уже ни к чему».
Редактор про поле ввода не знает, поэтому обязан доложить наверх, ЧТО именно убрали.
Оба крестика (принт и надпись) сведены в `PrintEditor._deleteLayer(d, wrap)`, оттуда
`onRemove(d)` → `TshirtApp.forgetLayer(d)`.
⚠️ Чистим только когда `liveTextId` указывает на ЭТОТ слой: поле описывает ровно одну
надпись, а крестик на чужой (она осталась на другой стороне) поле трогать не должен —
там текст ещё жив. Замер: до — поле «Смирнов», слоёв 1; после — поле пустое, слоёв 0,
`liveTextId: null`, `state.textInput: ''`.
⚠️ Порядок в конструкторе редактора важен: `onRemove` идёт ПЕРЕД `onChange`, потому что
`onChange` пересобирает панель и прочитал бы старое значение.

### 5. Раскрытые поля не схлопывались
Клиент: «нажал кнопку шрифт и цвет, там выбрал какой-то шрифт, а потом перешёл к другому
полю… в любое поле кликнул — то вот это поле, где добавить текст и шрифт, оно должно
сворачиваться… чтобы нам пространство не расширять… как у нас футболка сделана для
футбольной формы. Тоже самое с таблицей размеров… И то же самое с детализацией внизу».
Это НЕ обычный аккордеон «по одному за раз»: сворачивать обязан ЛЮБОЙ клик снаружи.
- Новый `src/js/ui/PanelAccordion.js` — состояние без DOM: `isOpen`, `toggle`,
  `openOnly` (нативный `<details>` умеет раскрыться сам), `closeIfOutside(isInside)`.
- `TshirtApp._registerPanel(name, root, apply)` / `_syncPanels()` / `_wireAccordion(doc)`.
  Зарегистрированы три поля: `text`, `sizes`, `details`. `renderPanel()` чистит `panelRefs`.
- ⚠️ **Слушатель только в фазе ПЕРЕХВАТА** (`addEventListener('click', fn, true)`): иначе
  клик по кнопке другого поля сперва открыл бы её, а сторож тут же закрыл. Тест это
  проверяет отдельным `assert.equal(capture, true)`.
- ⚠️ **Корнем панели регистрируется ВЕСЬ обёрточный `.field`, а не только тело.** Тогда
  клик по собственной кнопке считается внутренним, и панель закрывает только её же
  `toggle`. Побочно это же спасает выбор шрифта и перетаскивание пикера от схлопывания.
- ⚠️ `apply` для `<details>` присваивает `details.open` ТОЛЬКО при реальном расхождении,
  иначе `toggle`-событие уходит в бесконечную рекурсию.
- ⚠️ **Событие `toggle` у `<details>` асинхронное.** Замер целиком в одном синхронном
  `preview_eval` показывает `panels.open: null` при раскрытой детализации и выглядит как
  баг. Мерить только через `await` с паузой ≥60 мс.
Замер цепочкой: старт `null` → детализация `details` → шрифты `text` → таблица `sizes` →
детализация `details` → клик по футболке `null`. В DOM каждый раз раскрыт ровно один.
Отдельно проверено, что выбор шрифта, тычок в пикер и клик в поле ввода список НЕ сворачивают.

### Что осталось по этой передаче
1. Выложить на боевой (11+1 файл: добавился `src/js/ui/PanelAccordion.js`) и **поднять токен**
   на странице 3686. Заливка только из живого Chrome владельца.
2. Закоммитить: изменены `TshirtApp.js`, `PrintEditor.js`, `app.css`, добавлены
   `PanelAccordion.js` и тесты `PanelAccordion`, `PanelWiring`, `TextClearOnDelete`.
3. Отчёт клиенту (вместе с четырьмя пунктами от 25.08, он тоже не написан).

## Открытые хвосты (живые)
1. **Векторизация принта «РУКИ ВВЕРХ! THE BEST»** — отдельный платный разовый заказ того же
   клиента, файлы готовы и проверены замером (`docs/vector-rv/`). Ждёт двух слов клиента:
   **ширина печати в см** (сейчас `PRINT_W_CM = 25.0`) и **цена, рекомендовано 3 000 ₽**.
   Текст готов в `docs/vector-rv/otklik-klientu.md`, **не отправлен**. Как это сделано и
   какие там грабли — раздел «ПЕРЕДАЧА 17.08» в HISTORY.
   ⚠️ Сказано владельцу, НЕ клиенту: «Руки Вверх!» действующий товарный знак.
2. **КП по админке футболок не отправлено** (обсуждалось 18 000 ₽ отдельно / 30 000 ₽ за обе).
3. **Сантиметры детской зоны**: рамка тянется, а в заказ всё равно уходит 30×40. Спросить,
   если у клиента детская печать реально другого размера.
4. **Смена цвета футболки под уже положенный принт**: поведение не меняли, принт остаётся
   без предупреждения. Клиенту предложен вариант «оставить и показать заметку», ответа нет.
5. Мокапы: пилот переделки в `docs/mockup-pilot/` (Nano Banana Pro, IoU 98.7%). Решения нет.
6. Векторизация загружаемых покупателем JPEG→PNG — нужна ли услуга и по какой цене.
7. Пояснения к цветам: текст БЕЛОГО дословно от клиента, чёрный и слоновая кость написаны
   нами и ждут подтверждения. ⚠️ `keepNotes()` в `AdminOverrides` подставляет пояснение из
   базового конфига по `id`, иначе правка цвета через админку молча стирала бы текст.

## 🧰 Грабли инструментов (общие, стоили времени не по разу)
- **tdd-guard: у встроенного `node --test` репортера нет.** Гонять ТОЛЬКО через мост
  `node C:/Projects/.claude/tdd-guard/node-report.mjs tests/X.test.js` — он пишет
  `C:/Projects/.claude/tdd-guard/data/test.json` и повторяет код возврата. Голый `node --test`
  оставляет сторожу устаревшие данные, и он блокирует каждую правку. Мост печатает только
  сводку; какой именно тест упал — читать из `data/test.json`.
- **Отладочная ручка в браузере — `window.__tshirtApp`** (`main.browser.js:91`). `window.app`
  это DOM-узел `#app` и ловушка: `Object.keys` даёт `[]`, `state` = null.
- **Дескрипторы слоёв содержат `src` в виде data-URL.** `JSON.stringify` целого дескриптора
  выбивает лимит ответа (ловил 461 147 знаков). Фильтровать длинные строки до вывода.
- `location.reload()` внутри `preview_eval` убивает сам eval («Inspected target navigated»).
  Перезагрузка и замер — двумя отдельными вызовами.
- Живую правку поля ввода проверять НЕ значением, а тождеством узла
  (`document.querySelector(sel) === input`) и `document.activeElement`.
- Проверка синтаксиса через `new Function(код без import)` НЕ ловит дублирующийся импорт —
  он однажды уронил весь конструктор при зелёной проверке. Проверять загрузкой страницы.
- `Number(null)` даёт 0, а не NaN; `Number([1400])` даёт 1400. Пустая настройка молча
  превращалась в самое узкое поле.
- Замер тач-целей на 375px в браузере с МЫШЬЮ бессмысленен: `@media (pointer: coarse)`
  не активируется. Проверять через `matchMedia('(pointer: coarse)').matches`.
- Находимость элемента проверяется ГЕОМЕТРИЕЙ относительно обрезающего предка, а не
  доставкой событий: синтетика не видит `overflow: hidden` у родителя.
- Расшифровка видео и голосовых: `faster-whisper` (модель small, language=ru), ffmpeg из
  `imageio_ffmpeg`. Вывод писать в файл (кириллица в консоли ломается, `PYTHONIOENCODING=utf-8`).
- `cd` сбрасывается между вызовами Bash — пути абсолютные. Для curl `127.0.0.1`, не `localhost`.
- Edit требует предварительного Read того же файла.
- **Мост tdd-guard берёт ФАЙЛЫ, а не каталог**: `node .../node-report.mjs tests/` даёт
  `MODULE_NOT_FOUND`. Разворачивать глоб: `$(ls tests/*.test.js | tr '\n' ' ')`.
- **`javascript_tool` не ждёт промисов** — возвращает `undefined` или `{}`. Всегда в два
  вызова: первый кладёт результат в `window.__out` внутри `.then()`, второй читает
  `JSON.stringify(window.__out)`.
- **Ответ инструмента режется на строках с query string**: вернул адрес рамки с `?v=` —
  получил `[BLOCKED: Cookie/query string data]`. Возвращать только производные факты
  (числа, длины, булевы), а не сырые URL.
- **`file_upload` больше не принимает путь на диске**, требует содержимое в параметре
  `files`. Значит 200+ КБ base64 попадут в контекст: такую заливку делать СУБАГЕНТОМ.
- **Класс окна библиотеки — `libm`** (`libm__card`, `libm__cat`, `libm__cell`, `libm__thumb`,
  `libm__close`). Селектор `[class*="library"]` цепляет ЧУЖОЙ статичный элемент 74 px и даёт
  ложный вывод «position: static». Класс брать из `LibraryPanel.js`, а не из головы.
- `document.body` на странице wp-admin бывает `null` — писать защитно, иначе `TypeError`
  скрывает настоящую причину (например, что открыта форма входа).

## ⚠️ Правило работы с этим клиентом
**Перед утверждением «у клиента сломано X» открыть конструктор глазами и убедиться, что
покупатель это видит.** Дважды ошибался обратным порядком (в соседнем проекте формы),
клиент оба раза оказался прав. Он же 01.08: «давайте уже заканчивать… только вот сами
протестируйте, а то мы часто по кругу ходим». Поэтому каждый пункт закрывается ЗАМЕРОМ.
